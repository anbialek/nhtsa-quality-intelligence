name: nhtsa_quality_intelligence
version: 1.0.0
config-version: 2

profile: nhtsa_quality_intelligence

model-paths: ["models"]
macro-paths: ["macros"]
test-paths: ["tests"]
seed-paths: ["seeds"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  nhtsa_quality_intelligence:
    staging:
      +materialized: view
      +schema: staging
    silver:
      +materialized: table
      +schema: silver